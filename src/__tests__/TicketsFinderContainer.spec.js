'use strict';

import expect from 'expect';
import React from 'react';
import {shallow, mount} from 'enzyme';
import {TicketsFinderContainer} from '../containers/TicketsFinderContainer';

decribe('TickerFinderContainer component', function () {

    let _wrapper;

    beforeEach(function () {
        _wrapper = mount(<TicketsFinderContainer/>);
    });

});